{
  "name": "magenais",
  "version": "2.1.0",
  "description": "MAGENAIS – GEN AI Operating System (Browser‑First, Plugin‑Driven)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "node server/proxy-server.mjs",
    "test": "node --experimental-strip-types --test tests/unit/*.test.ts",
    "check-providers": "node scripts/check-providers.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  },
  "license": "Apache-2.0"
}
