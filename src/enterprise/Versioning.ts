/**
 * Versioning utilities – can be expanded later for fine-grained version control.
 * Currently, versioning is handled by ProjectManager and AssetManager.
 */
export class Versioning {
  // Placeholder – future: diffing, rollback, etc.
}
